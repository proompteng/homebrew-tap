# proompteng Homebrew Tap

Install:

```bash
brew tap proompteng/tap
brew install proompteng/tap/agentctl
```

`agentctl` binaries are published from `proompteng/lab` releases tagged `agentctl-vX.Y.Z`.
